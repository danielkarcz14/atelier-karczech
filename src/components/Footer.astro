---
interface NavLink {
  label: string;
  href: string;
}

interface Props {
  logoPlain: string;
  homeHref?: string;
  logoAccent: string;
  links: NavLink[];
  copyright: string;
}

const { logoPlain, logoAccent, links, copyright, homeHref = '#' } = Astro.props;
const year = new Date().getFullYear();
---

<footer class="border-t border-brand-800/30">
  <div class="page-shell py-12">
    <div class="flex flex-col md:flex-row items-center justify-between gap-8">
      <a href={homeHref} class="font-display text-xl font-semibold tracking-[-0.01em] text-white">
        {logoPlain}<span class="text-accent">{logoAccent}</span>
      </a>
      <div class="flex flex-wrap justify-center gap-8">
        {
          links.map((link) => (
            <a href={link.href} class="nav-link text-sm text-brand-500 hover:text-white transition-colors">
              {link.label}
            </a>
          ))
        }
      </div>
      <div class="text-xs text-brand-600">
        &copy; {year} {copyright}
      </div>
    </div>
  </div>
</footer>
